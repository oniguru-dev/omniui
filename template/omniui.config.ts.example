/**
 * omniui — Lightweight UI Framework
 * Built with Preact & UnoCSS | @omnidev
 *
 * Configuration file.
 */

const config = {
  baseUrl: "http://localhost:8080",
  port: 8080,                     // listen on port :8080
  upnp: false,                    // upnp port forwarding
  local: false,                   // local network access
  browser: true,                  // open browser window

  robots: { // robots.txt | auto-generated
    crawler: 2,             // rate-limit (default: 2)
    disallow: [ "/api/*" ], // robots.txt
    ignore: [ "/api/*" ],   // sitemap.xml
  },

  routing: {
    strictCase: false,       // true = /About ≠ /about
  },

  i18n: { // internationalization
    defaultLocale: "en",     // default language
    locales: ["en", "ru"],   // available locales
    cookie: "locale",        // cookie name for locale persistence
  },
};

export default config;
